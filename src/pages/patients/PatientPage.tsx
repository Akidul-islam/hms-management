import { Box, Typography } from '@mui/material';
import * as React from 'react';

const PatientPage: React.FC = () => {
  return (
    <Box component={'section'}>
      <Typography>Patient Dashborad</Typography>
    </Box>
  );
};

export default PatientPage;
